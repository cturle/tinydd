(ns repl.race
  (:require [tinydd.race :as r]) )

; - add a new race with name = "elf"
; check : a model is loaded
(defn add-race [name]
  ;todo
  )


; - list all races, displaying their name
; check : a model is loaded
(defn display-many-race [RACES])


(defn get-all-race [])
